import React, { useEffect } from 'react';

interface Props { }

function Home(props: Props) {
    const { } = props;

    // useEffect(() => {
    //     (async () => {
    //         const test = await fetch('/api/health');
    //         console.log(test);
    //     })();
    // });

    return (
        <div>
            Home
        </div>
    );
}

export default Home;
